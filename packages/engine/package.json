{
  "name": "@brickify/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pipeline pur TypeScript : image -> silhouette -> voxels -> briques LEGO -> instructions. Zéro dépendance runtime.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "demo": "node --import tsx src/demo/run.ts",
    "typecheck": "tsc --noEmit"
  }
}
